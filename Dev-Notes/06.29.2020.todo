June 29th, 2020

Rhode Island


Solution to the switch statement, ES6, JSX, React problem:
  - Conditional Rendering... derp
  https://reactjs.org/docs/conditional-rendering.html

  ☐ Apply this to the current sidebar/content module.

  I think this is the best way to define the different elements asked for in the buttons.
  function LoginButton(props) {
    return (
      <button onClick={props.onClick}>
        Login
      </button>
    );
  }

  function LogoutButton(props) {
    return (
      <button onClick={props.onClick}>
        Logout
      </button>
    );
  }  


  Ended up trying to create functions in global scope above react component declaration. They work for just static markup being brought into the render function within the component, but as far as passing refs through props up to the random function declared above... it doesn't work, I'm pretty sure it's not designed to be that way, nearly certain.

  inside of src/sub/modes I've place each mode component. This is where they will all be imported from to be included into the si9debar component